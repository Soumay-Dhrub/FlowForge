name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  backend-test:
    name: Backend Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Java 21 is the project's target and the newest release Spring Boot 3.2.5 supports;
      # its repackage goal cannot read class files from a newer JDK.
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # Unit tests and the jqwik property tests. Surefire excludes the "integration" group, so this
      # stays fast and needs no Docker.
      - name: Run backend unit and property tests
        working-directory: ./backend
        run: mvn verify

      # The integration tests are the ones that catch what unit tests structurally cannot: this project
      # has already shipped a draft-save bug that every in-memory test passed, and an attachment upload
      # that only failed against a real container. They were excluded from the command above, so
      # without this step they had never run in CI at all.
      #
      # Testcontainers needs a Docker daemon, which ubuntu-latest provides. All the integration classes
      # share one PostgreSQL container via IntegrationTestBase, so this costs one startup rather than one
      # per class.
      - name: Run backend integration tests
        working-directory: ./backend
        run: mvn verify -Pintegration -DfailIfNoTests=false

      - name: Upload backend test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-results
          path: |
            backend/target/surefire-reports/
            backend/target/failsafe-reports/

  frontend-test:
    name: Frontend Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Cache pnpm dependencies
        uses: actions/cache@v4
        with:
          path: |
            frontend/node_modules
            ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        working-directory: ./frontend
        run: pnpm install --frozen-lockfile

      - name: Run linter
        working-directory: ./frontend
        run: pnpm run lint

      - name: Run frontend tests
        working-directory: ./frontend
        run: pnpm run test:ci

      - name: Upload frontend test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-test-results
          path: frontend/coverage/

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-test]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Every build is tagged with the commit SHA, which is the only tag that identifies exactly what
      # is running. `latest` is added on main as well, so a deployment can follow the branch, but a
      # moving tag is never the only one an image has: given a running container you can always get
      # back to the commit it was built from.
      - name: Work out the image tags
        id: tags
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "backend=flowforge-backend:${{ github.sha }},flowforge-backend:latest" >> "$GITHUB_OUTPUT"
            echo "frontend=flowforge-frontend:${{ github.sha }},flowforge-frontend:latest" >> "$GITHUB_OUTPUT"
          else
            echo "backend=flowforge-backend:${{ github.sha }}" >> "$GITHUB_OUTPUT"
            echo "frontend=flowforge-frontend:${{ github.sha }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build backend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: false
          tags: ${{ steps.tags.outputs.backend }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # API_PROXY_TARGET is not passed here on purpose: the Dockerfile defaults it to
      # http://backend:8080, the compose service name. It is a BUILD-time value -- Next freezes rewrite
      # destinations into routes-manifest.json -- so deploying somewhere the backend has a different
      # address means rebuilding with a different arg, not setting an environment variable.
      - name: Build frontend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: false
          tags: ${{ steps.tags.outputs.frontend }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  status-check:
    name: CI Status Check
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-test, docker-build]
    if: always()
    
    steps:
      - name: Check all jobs
        run: |
          if [[ "${{ needs.backend-test.result }}" != "success" ]]; then
            echo "Backend tests failed"
            exit 1
          fi
          if [[ "${{ needs.frontend-test.result }}" != "success" ]]; then
            echo "Frontend tests failed"
            exit 1
          fi
          if [[ "${{ needs.docker-build.result }}" != "success" ]]; then
            echo "Docker build failed"
            exit 1
          fi
          echo "All CI checks passed!"
